// DEPRECATED: the `plugins` compatibility layer has been removed.
// Adapters are imported directly from `src/adapters/*`.
// This file is left as a harmless shim to avoid accidental imports. Do not rely on it.

export default undefined

